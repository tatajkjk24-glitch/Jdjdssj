-- =======================================
-- Script Name: هادي لبطل
-- Target Game: Blade Ball (كرة الشفرة)
-- Executor: Delta
-- Features: Target Only Auto Parry + Anti-Kick + ON/OFF GUI
-- =======================================

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local VirtualInputManager = game:GetService("VirtualInputManager")
local CoreGui = game:GetService("CoreGui")
local LocalPlayer = Players.LocalPlayer

local ScriptTitle = "هادي لبطل"
local AutoParry = true
local DistanceMultiplier = 0.65

-- =======================================
-- 🛡️ نظام الحماية من الطرد (Anti-Kick)
-- =======================================
task.spawn(function()
    if hookmetamethod then
        local oldNamecall
        oldNamecall = hookmetamethod(game, "__namecall", newcclosure(function(self, ...)
            local method = getnamecallmethod()
            if tostring(method):lower() == "kick" and self == LocalPlayer then
                warn("[" .. ScriptTitle .. "] : تم حظر محاولة طردك من السيرفر بنجاح!")
                return nil
            end
            return oldNamecall(self, ...)
        end))
    end

    if hookfunction and LocalPlayer and LocalPlayer.Kick then
        hookfunction(LocalPlayer.Kick, newcclosure(function(self, ...)
            warn("[" .. ScriptTitle .. "] : تم حظر محاولة طرد مباشرة!")
            return nil
        end))
    end
end)

-- =======================================
-- 🎨 إنشاء واجهة المستخدم (GUI)
-- =======================================
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "HadiHeroGui"
ScreenGui.ResetOnSpawn = false

if gethui then
    ScreenGui.Parent = gethui()
elseif syn and syn.protect_gui then
    syn.protect_gui(ScreenGui)
    ScreenGui.Parent = CoreGui
else
    ScreenGui.Parent = CoreGui or LocalPlayer:WaitForChild("PlayerGui")
end

local ToggleButton = Instance.new("TextButton")
ToggleButton.Name = "ToggleButton"
ToggleButton.Parent = ScreenGui
ToggleButton.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
ToggleButton.BorderSizePixel = 0
ToggleButton.Position = UDim2.new(0.05, 0, 0.2, 0)
ToggleButton.Size = UDim2.new(0, 150, 0, 45)
ToggleButton.Font = Enum.Font.SourceSansBold
ToggleButton.Text = ScriptTitle .. "\n[ مفعل 🟢 ]"
ToggleButton.TextColor3 = Color3.fromRGB(0, 255, 127)
ToggleButton.TextSize = 16
ToggleButton.Active = true
ToggleButton.Draggable = true

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 10)
UICorner.Parent = ToggleButton

local UIStroke = Instance.new("UIStroke")
UIStroke.Thickness = 2
UIStroke.Color = Color3.fromRGB(0, 255, 127)
UIStroke.Parent = ToggleButton

ToggleButton.MouseButton1Click:Connect(function()
    AutoParry = not AutoParry
    if AutoParry then
        ToggleButton.Text = ScriptTitle .. "\n[ مفعل 🟢 ]"
        ToggleButton.TextColor3 = Color3.fromRGB(0, 255, 127)
        UIStroke.Color = Color3.fromRGB(0, 255, 127)
    else
        ToggleButton.Text = ScriptTitle .. "\n[ معطل 🔴 ]"
        ToggleButton.TextColor3 = Color3.fromRGB(255, 65, 65)
        UIStroke.Color = Color3.fromRGB(255, 65, 65)
    end
end)

-- =======================================
-- ⚽ تحديد الكرة الموجهة للاعب
-- =======================================
local function GetActiveBall()
    local ballsFolder = workspace:FindFirstChild("Balls")
    if not ballsFolder then return nil end
    
    for _, ball in pairs(ballsFolder:GetChildren()) do
        if ball:GetAttribute("realBall") == true or ball:FindFirstChild("zoomies") or ball.Name == "Ball" then
            return ball
        end
    end
    return nil
end

local function ExecuteParry()
    VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.F, false, game)
    task.wait(0.001)
    VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.F, false, game)
end

-- =======================================
-- ⚡ حلقة التصدّي التلقائي السريع
-- =======================================
RunService.PreRender:Connect(function()
    if not AutoParry then return end
    
    local character = LocalPlayer.Character
    if not character or not character:FindFirstChild("HumanoidRootPart") then return end
    
    local hrp = character.HumanoidRootPart
    local ball = GetActiveBall()
    
    if ball then
        -- التصدّي حصرياً عندما يكون اسمك هو الموجه له الكرة في الماب
        local targetName = ball:GetAttribute("target") or ball:GetAttribute("Target")
        local isDirectlyTargeted = (targetName == LocalPlayer.Name)
        
        if isDirectlyTargeted then
            local ballPos = ball.Position
            local playerPos = hrp.Position
            local distance = (ballPos - playerPos).Magnitude
            
            local velocity = ball.AssemblyLinearVelocity
            local speed = velocity.Magnitude
            
            local dynamicTriggerDistance = math.clamp(speed * DistanceMultiplier, 12, 120)
            
            if distance <= dynamicTriggerDistance then
                ExecuteParry()
            end
        end
    end
end)
 
